export type ICharacter = {
    created: string
    id: string
    image: string
    name: string
    status: string
}

export type IInfo = {
    count: number | null
    next: number | null
    pages: number | null
    prev: number | null
}
